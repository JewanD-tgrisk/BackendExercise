def integers_above_below(the_list, value):
    
    integers = {'above': 0, 'below': 0}

    for element in the_list:
        if element > value:
            integers['above'] += 1
        elif element < value:
            integers['below'] += 1
    
    return integers

    from abovebelow import count_above_below

print(integers_above_below([1, 5, 2, 1, 10], 6))
# -> {'above': 1, 'below': 4}